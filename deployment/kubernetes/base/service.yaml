apiVersion: v1
kind: Service
metadata:
  name: cloud-cost-optimizer-service
  namespace: finops-optimizer
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: cloud-cost-optimizer
