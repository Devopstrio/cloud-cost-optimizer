apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-cost-optimizer
  namespace: finops-optimizer
  labels:
    app: cloud-cost-optimizer
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cloud-cost-optimizer
  template:
    metadata:
      labels:
        app: cloud-cost-optimizer
    spec:
      containers:
        - name: cloud-cost-optimizer
          image: devopstrio/cloud-cost-optimizer:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: optimizer-config
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "2048Mi"
